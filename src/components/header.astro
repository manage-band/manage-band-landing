---
import type { NavItem } from '@/types';

const navItems: Array<NavItem> = [
  { title: 'Funkcjonalności', url: '#features' },
  { title: 'Dla kogo', url: '#for-who' },
  { title: 'O nas', url: '#about-us' },
];
---

<header
  id='page-header'
  class='absolute top-0 z-20 flex w-full justify-center border-b border-transparent px-8 py-8 text-white'
>
  <div class='container flex justify-between items-center h-[32px]'>
    <a class='flex items-center gap-4' href='#'>
      <h1 class='sr-only'>Manage.Band</h1>
      <img src='favicon.png' alt='Manage.Band' height='24px' width='24px' />
      <h1 class='text-2xl font-semibold hidden sm:block'>
        <span>Manage.</span><span class='text-brandRed'>Band</span>
      </h1>
    </a>
    <nav class='hidden sm:block' id='main-navigation'>
      <ul class='flex items-center gap-8'>
        {
          navItems.map(({ title, url }) => (
            <li>
              <a class='text-md' href={url}>
                {title}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>
