---
export type Props = {
  href: string;
  label: string;
};

const { href, label } = Astro.props;
---

<a href={href}>
  <button
    class='hover:bg-brand-tertiary bg-brand rounded-lg px-7 py-3 text-white transition-colors duration-100 ease-in-out'
    >{label}</button
  >
</a>
