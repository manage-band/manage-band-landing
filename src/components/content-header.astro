---
export type Props = {
  title: string;
  description?: string;
};

const { title, description } = Astro.props;
---

<div class='w-full space-y-8 md:w-2/3'>
  <h2 class='text-center'>
    {title}
  </h2>
  {description && <p class='text-md px-8 text-center'>{description}</p>}
</div>
