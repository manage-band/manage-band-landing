export type Feature = {
  title: string;
  img: string;
  background?: string;
  description: string;
  styling?: string;
  backgroundImage?: string;
};

export type Paragraph = {
  content: string;
  styling?: string;
};
