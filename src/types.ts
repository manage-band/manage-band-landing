export type NavItem = {
  title: string;
  url: string;
};

export type Goal = {
  title: string;
  img: string;
  background?: string;
  description: string;
  styling?: string;
};

export type Paragraph = {
  content: string;
  styling?: string;
};
